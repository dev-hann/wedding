import 'package:flutter/material.dart';

class AttendModal extends StatelessWidget {
  const AttendModal({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text("Attend Modal");
  }
}
